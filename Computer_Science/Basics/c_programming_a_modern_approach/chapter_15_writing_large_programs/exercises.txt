1) Section 15.1 listed several advantages of dividing a program into multiple source files.

a) Describe several other advantages
    - easier to maintain
    - you can reuse some file in other programs
    - abstraction
b) Describe some disadvantages
    - you can't compile program with just "make <c_file>"
    - can be harder to understant for someone who works only with one source-file


############################

2) Which of the following should NOT be put in a header file? Why not?

a) Function prototypes -> OK
b) Function definitions -> NO (it will cause problem with linking)
c) Macro definitions -> OK
d) Type definitions -> OK

#################################

3) We saw that writing #include <file> instead of #include "file" ay not work if 
    file is one that we've written. Would there be any problem with writing #include "file" 
    instead of #include <file> if file is a system header?

    - it will compile because when it didn't find the #include "file" in current directory, it will search in system file path

###################################

4) Assume that debug.h is a header file with the following contents:

#ifdef DEBUG
#define PRINT_DEBUG(n) printf("Value of " #n ": %d\n", n)
#else
#define PRINT_DEBUG(n)
#endif

###### AND testdebug.c_file
#include <stdio.h>
#define DEBUG
#include "debug.h"

int main(void) {
    int i = 1, j = 2, k = 3;
#ifdef DEBUG
    printf("Output if DEBUG is defined:\n");
#else
    printf("Output if DEBUG is not defined:\n");
#endif

    PRINT_DEBUG(i);
    PRINT_DEBUG(j);
    PRINT_DEBUG(k);
    PRINT_DEBUG(i + j);
    PRINT_DEBUG(2 * i + j - k);
    return 0;
}

a) What is the output when the program is executed?

Output if DEBUG is defined:
Value of i: 1
Value of j: 2
Value of k: 3
Value of i + j: 3
Value of 2 * i + j - k: 1


b) What is the output if the #define directive is removed from testdebug.c?

    Output if DEBUG is not defined:

c) Explain why the output is different in parts "a" and "b"

    1) If debug is not defined, it will print other print message
    2) If debug is not defined PRINT_DEBUG(n) do nothing

d) Is it nescessary for the DEBUG macro to be defined before debug.h is included in order
    for PRINT_DEBUG to have the desired effect? Justify your answer

    - Yes, because when header file check the condition, debug is not defined,
      and PRINT_DEBUG do nothing.

##############################

5) Suppose that a program consists of three source files:
    - main.c
    - f1.c
    - f2.c

    and two header files:
    - f1.h
    - f2.h

    All 3 source files include f1.h, but only f1.c and f2.c
    include f2.h
    Write a makefile for this program assuming that the compiler
    is gcc and that the executable is to be named demo

    demo: main.o f1.o f2.o
        gcc -o demo main.o f1.o f2.o

    main.o: main.c f1.h
        gcc -c main.c

    f1.o: f1.c f1.h f2.h
        gcc -c f1.c

    f2.o: f2.c f1.h f2.h
        gcc -c f2.c

##################################

6) The following questions refer to the program described in exercise 5.

a) Which files need to be compiled when the program is built for the first time?
    - f1.c, f2.c, main.c

b) If f1.c is changed after the program has been built, which files need to be recompiled?
    - f1.c

c) If f1.h is changed after the program has been built, which files need to be recompiled?
    - f1.c, f2.c, main.c

d) If f2.h is changed after the program has been built, which files need to be recompiled?
    f1.c, f2.c
